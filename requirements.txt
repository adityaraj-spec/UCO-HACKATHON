# ============================================================
# PhaseGuard — Layer 2 (Voice-Based Authentication) Requirements
# Production-grade banking software dependencies
# Tested on Python 3.14 (Windows x64)
# ============================================================

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Database / ORM ---
sqlalchemy>=2.0.30      # 2.0.51 confirmed on Python 3.14
# asyncpg==0.29.0       # Requires C build tools on Windows; install separately
# psycopg2-binary==2.9.9 # Optional: only needed if using sync psycopg2
alembic==1.13.1
pgvector==0.2.5

# --- Settings / config ---
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- ML / audio ---
torch==2.2.2
torchaudio==2.2.2
speechbrain==1.0.0
soundfile==0.12.1
librosa==0.10.1
numpy==1.26.4
scipy==1.13.0

# --- Vector search ---
faiss-cpu>=1.14.3       # 1.14.3+ has Python 3.14 wheels for Windows

# --- Cache / pub-sub ---
redis==5.0.4
kafka-python>=3.0.7     # 3.0.7+ has Python 3.14 compatible wheels

# --- Security / Cryptography ---
cryptography==42.0.8
pyjwt[crypto]==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# --- Rate limiting ---
slowapi==0.1.9

# --- Frontend ---
streamlit==1.35.0
plotly==5.22.0
requests==2.32.3

# --- HTTP client ---
httpx==0.27.0

# --- Logging / utils ---
loguru==0.7.2
python-json-logger==2.0.7

# --- Testing ---
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
faker==25.0.0

# --- Background tasks ---
celery==5.4.0
